import { SearchByPricePipe } from './search-by-price.pipe';

describe('SearchByPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByPricePipe();
    expect(pipe).toBeTruthy();
  });
});
